name: demo_core
description: Shared validation and demo logic for package:tailscale examples.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.2.0
  tailscale:
    path: ../..
  tailscale_profile_harness:
    path: ../../benchmark/profile_harness

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
