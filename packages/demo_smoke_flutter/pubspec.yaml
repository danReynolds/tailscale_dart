name: dune_smoke_flutter
description: Automated Flutter smoke probe for package:tailscale.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  demo_core:
    path: ../demo_core
  ffi: ^2.1.0
  flutter:
    sdk: flutter
  path: ^1.9.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

hooks:
  user_defines:
    tailscale:
      profile_diagnostic: true
