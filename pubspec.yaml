name: tailscale
description: A custom Tailscale Userspace Networking bridge for Dune.
publish_to: none
version: 0.0.3
environment:
  sdk: ^3.10.4

dependencies:
  ffi: ^2.1.0
  meta: ^1.9.0

hooks:
  hooks: ^1.0.0
  code_assets: ^1.0.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.24.0
  lints: ^2.0.0
  code_assets: ^1.0.0
  hooks: ^1.0.0
  path: ^1.9.0
