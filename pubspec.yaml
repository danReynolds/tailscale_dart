name: tailscale
description: Embed Tailscale userspace networking in any Dart or Flutter app. Join a tailnet, discover peers, and communicate over encrypted WireGuard tunnels — no Tailscale app required.
version: 0.1.0
repository: https://github.com/dan/tailscale_dart

environment:
  sdk: ^3.10.4

dependencies:
  ffi: ^2.1.0
  http: ^1.2.0

hooks:
  hooks: ^1.0.0
  code_assets: ^1.0.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.24.0
  lints: ^2.0.0
  code_assets: ^1.0.0
  hooks: ^1.0.0
  path: ^1.9.0
